// const fs = require("fs");
// const jsdom = require('jsdom');
// const jQuery = require("jquery");
// const dateFormat = require('dateformat');

module.exports = (obj) => {
    const res = obj.res;
    const file = obj.file;
    console.log('lib/index.js: ', obj);
    return obj;
};
